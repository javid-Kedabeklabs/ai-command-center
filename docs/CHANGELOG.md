# Changelog

## Unreleased — autonomous implementation contract

- Verified recovery baseline and repository-managed Codex supervisor.
- Added schema-v2 lossless migration, typed ports, runtime schema validation, and previews.
- Added dependency scheduling, bounded parallelism, run scopes, checkpoints, retry branch, cancellation, and restart recovery.
- Added persisted groups and executable subworkflow extraction.
- Added MCP stdio/Streamable HTTP discovery and execution.
- Added persistent interval/cron/webhook/folder triggers with security and deduplication.
- Added custom-node wizard/versioning and plugin trust enforcement.
- Added Evaluation Lab, run attachment, learning proposals, and promotion gates.
- Added local-only/permission enforcement and Level 1 Company World Operations Map.
- Host-verified durable cron/interval cursor semantics, restart stability, overlap/misfire policies, and trigger regression gates.
- Added the controlled Codex-lead/Claude-worker architecture amendment, fail-closed actual-model provenance, and the first verified Fable modifying pilot through isolated commit, Codex review, tests/build, and selective integration.
- Added semantic autonomy blocker/no-progress controls and a deterministic typed host-operation queue/controller; used it to create and verify an external backup, reload safely, and complete folder-trigger host verification.
- Added mechanically source-cited local-model findings and a repeatable known-answer A/B benchmark; the existing Qwen baseline is recorded before any replacement decision.
- Added generation-guarded webhook credential rotation and permanent revocation, bearer-header delivery, atomic credential verification plus deduplicated reservation, strict secret-record validation, one-time UI disclosure, restart tests, and a reload controller that waits for healthy localhost.
- Added metadata-only macOS Keychain secret references with no-store CRUD, revision-safe rotation, usage-protected deletion, HTTP and remote MCP bearer resolution at the transport boundary, inline-credential rejection, minimal provider child environments, truthful UI selection/management, and live non-persistence/lock/permission verification.
- Added a copy-ready repository/skills research brief aligned to the verified architecture, near-term critical path, model-worker roles, dependency/security filters, and deferred 3D scope.
- Deferred specialized long-form document production by owner direction; engineering documentation remains required, while implementation capacity stays on the core product.
- Added the Agent Primitive/Role Card architecture amendment and additive 14-primitive schemas, resolver isolation, and nondestructive migration preview; no legacy data migration yet.
- Added immutable candidate/run-bound evaluation baselines, versioned datasets, artifact-backed visual/accessibility/security/model reports, and replay-safe controlled-learning proposals that can create only separate governed Development candidates.
- Added portable reviewed plugin packages, durable Company World departments, versioned install/upgrade/rollback, and sanitized package/SBOM/clean-install gates.
- Completed Workflow Studio copy/paste/duplicate/delete/undo/search/Escape interactions, reduced-motion behavior, responsive containment, live accessibility coverage, and deterministic macOS canvas visual regression.
- Added disk-full fault injection before and after rename, explicit commit-uncertain write errors, bounded concurrency/checkpoint soak, DAG and atomic-write performance ceilings, and descriptor/temp-file leak checks to the 26-gate release verifier.

This is not a final release; later phases remain open in `docs/IMPLEMENTATION_STATUS.md`.
