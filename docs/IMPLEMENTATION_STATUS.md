# Implementation Status

Living document. Updated at each phase.

## Autonomous implementation contract (2026-07-13)

| Phase | Status | Evidence |
|---|---|---|
| 0 — Recovery and stabilization | VERIFIED | Checksummed backup; Node/TypeScript/build clean; original 30/30 smoke; interrupted-capability 26/26; localhost live. |
| 1 — Autonomous supervisor | VERIFIED | Bounded-runner lifecycle 7/7, semantic control plane 8/8, and deterministic host controller 7/7 cover single instance, timeout, capacity/stop behavior, one-dispatch host waiting, blocker fingerprints, no-progress halt, typed idempotent host requests, auditable request supersession, safe backup/verification, reload preconditions, and a required post-reload localhost health condition. Supported `codex exec` flags, workspace-write sandbox, private bounded logs, durable enabled/stop markers, crash-restarting LaunchAgent PathState, and terminal-state disable are implemented. |
| 2 — Canonical schema and typed ports | VERIFIED | Lossless schema-v2 migrations 5/5; full port vocabulary/compatibility/schema validation 5/5; persisted typed handles and runtime edge validation; future-schema rejection. |
| 3 — Durable execution | VERIFIED | Dependency scheduler 5/5; per-run coordinator 5/5; process-global model scheduler 8/8; fresh integration 3/3; terminal record/redaction 2/2; recovery 26/26; security 4/4; smoke 30/30. Reloaded PID 36368 serves the current repository. FIFO global bounds, explicit-memory admission, unknown-estimate fallback, stop/failure/shutdown release, checkpoints, retry/resume/selected/branch execution, restart recovery, and durable cache are verified. |
| 4 — Subworkflows and organization | VERIFIED | Nested execution, recursion/depth rejection, persistent hierarchical groups, exact version pins, reusable lifecycle, anchored non-executable comments, and non-escalating parent safety ceilings are implemented. Versioned component manifests export deterministic exact dependency closures, quarantine staged imports, require explicit review, reject tampering/secrets/cycles/missing dependencies/path traversal/collisions, and never replace locked production (10/10). A live Playwright journey now saves a reusable component, exports its exact manifest, deletes the source, stages the package without mutation, approves it, verifies the restored canonical definition, and sees it in the Reusable library. |
| 5 — MCP, tools, and triggers | VERIFIED | MCP registry/client 12/12 and live transports 4/4 pass. Keychain-backed opaque auth references resolve only at transport boundaries and pass registry/API/runtime/live 4/4 + 5/5 + 5/5 + 5/5. Trigger evidence includes store 17/17, schedules 13/13, live API 10/10, folder 9/9 + service 2/2 + two-restart recovery 5/5, security 4/4, recovery 36/36, smoke 30/30, and manager/API 6/6. Workflow Settings manages interval, cron, webhook, and folder definitions through canonical APIs, including truthful disabled/revoked/needs-review states and persisted history. The integrated Playwright/Axe suite covers these states plus deleted-trigger history and passed 90/90 repeated runs. No trigger or credential is copied into workflow draft state. |
| 6 — Custom nodes and plugins | VERIFIED | Executable no-code custom-node wizard, typed manifests, version history, disabled/untrusted import, exact manifest-bound review receipts, enable/disable/uninstall propagation, and runtime enforcement are implemented. Portable JSON packages enforce semantic compatibility, verify optional Ed25519 integrity signatures without confusing signatures with trust, omit local authority on export, and expose deterministic manifest/adapter/fixture checks. Package logic passes 8/8 and the live browser journey proves import, stale-review rejection, approval receipt, package test/export, and contributed-node provenance. |
| 7 — Evaluations and learning | VERIFIED_FOR_LOCAL_FIRST_V1 | Promotable results require exact immutable candidate runs; ad hoc, stale, incomplete, path-invalid, and cross-candidate evidence fail closed. Versioned datasets support exact/contains/canonical-JSON cases and aggregate receipts binding every case to the same candidate. Suites gate duration/reconciliation state, support baselines, and consume SHA-bound visual/accessibility/security/model reports; model reports require evaluator/rubric identity and can never be the sole production judge. Controlled learning fingerprints/deduplicates findings, distinguishes transient from deterministic failure, rejects stale sources, persists replay-safe decisions, and creates only separate Development candidates. The source never mutates automatically. The live journey binds exact regression evidence back to the approved proposal and promotes the immutable candidate into Testing through the governed lifecycle. Evaluation passes 14/14, datasets 4/4, learning 7/7 plus 2/2 live, and remains covered by the current release gate. |
| 8 — Governance | VERIFIED_FOR_LOCAL_FIRST_V1 | The exact Development/Testing/Production lifecycle, immutable candidates, exact evaluation evidence, deployment/rollback receipts, truthful lifecycle UI, durable per-node recovery/approval/manual pause, loopback request boundary, centralized secret redaction, deny-wins capabilities, and safe file boundaries are implemented. External APIs, MCP, webhooks, and remote models remain available as explicit destination- and environment-controlled capabilities; local-first means local ownership, not offline-only. Ambiguous effects durably stop for review. Production executable authority fails closed under the reviewed capability ceiling rather than relying on an unsupported macOS sandbox. The release gate proves multi-crash recovery, replay-safe control, trigger receipts, secret canaries, browser governance, and no critical/high production dependency vulnerability. |
| Collaboration amendment | IN_PROGRESS | Installed Claude Code 2.1.207/auth and Fable availability are verified. Schema/ownership/result/capacity tests pass 23/23, contract-freeze checks 4/4, runner/model-provenance/timeout gates 11/11, task persistence/process recovery and atomic ownership checks 12/12, durable sparse worktree/integration controls 11/11, API boundary 6/6, dispatch receipts 5/5, and accepted-delivery metrics 5/5. New schema-v2 packets freeze and verify the exact reviewed HEAD, acceptance-test ancestor, contract-file hashes, stable scenarios, file budget, baseline estimate, and stop conditions before task reservation; legacy v1 records remain readable but cannot be newly queued or dispatched. Worktree leases persist atomically, reconcile exact Git identity after restart, block orphan/mismatch state, preserve unknown or unintegrated work, and exclude `data/`, `logs/`, and `state/` from modifying workers. The dispatcher binds the contract verification, task, reviewed base, lease, owned PID, actual model provenance, inspected scoped commit, and terminal hash receipt. It also rejects unreviewed primary source dirt for read-only work. Live product dispatch requires host-owner opt-in plus exact request confirmation; automatic integration remains impossible. The atomic metrics store derives timing from receipts, requires explicit integration/rejection disposition, records escaped defects append-only, rejects symlink-parent escape, isolates corrupt evidence without overwriting it, and exposes only shadow Fable/Qwen eligibility after conservative sample/quality gates. It never changes authority or routing. The first real modifying Fable pilot proved `claude-fable-5` with no fallback and a reviewed isolated commit. Comparable task observations remain to be accumulated. |
| Local model factory amendment | IN_PROGRESS | Qwen3-Coder-30B remains pinned as `qwen-coder-factory` with 65,536 context and four slots. Findings now require mechanically verified file/range/exact-quote evidence; false citations are discarded and exact duplicates collapse. Deterministic tests pass 14/14 plus API 1/1. The repeatable six-case baseline at four-way concurrency recorded 100% schema validity, 1.00 precision, 0.60 recall, 0.75 F1, zero false positives, and 11.3 seconds. Qwen3-Coder-Next is downloading for a gated A/B comparison and will not replace the active model unless it improves the declared metrics. Patch/worktree execution, detailed result UI, resource telemetry, and supervisor dispatch remain disabled. |
| Agent primitive amendment | VERIFIED | Fourteen additive primitives, versioned Role Cards, separate instruction profiles and Agent Instances, exact Workflow Assignments, revisioned atomic persistence, nondestructive snapshot-bound preview, explicit ambiguity decisions, idempotent migration commands, durable rollback receipts, exact-only reactivation, legacy runtime compatibility, Agent UI, and Company World identity are implemented. Evidence: primitives 12/12, store 10/10, live API 6/6, and the live Playwright/Axe migration/rollback journey. |
| 9 — Complete UX and productivity | VERIFIED_FOR_CURRENT_SCOPE | Command palette, run/search/explain shortcuts, copy/paste with internal connections, duplicate/delete/undo/Escape, smart insertion, contextual actions, actionable node/branch recovery cards, schemas/consoles, responsive containment, reduced motion, and a stable visual baseline are implemented. Twelve live Playwright journeys cover all modes, keyboard editing, Agent Architecture migration, AI Collaboration truthfulness, governance, approvals, components, triggers, plugins, Company World, accessibility, and exact evidence. Unimplemented edge monitoring and direct Python diff generation are explicitly disabled with reasons. |
| 10 — 2D Employee Operations Map | VERIFIED | The map renders only persisted checkpoint/control/run facts, exact workflow recovery identity, real agents, resources, and bounded queues; no latest-event inference or synthetic progress remains. Task assignment/cancellation and navigation are live. Department definitions are now durable, revision-checked, auditable, authorable in the UI, and deletable only while empty; workers remain assigned through canonical Agent configuration. Pure organization checks pass 9/9 and Playwright/Axe proves empty departments never invent work. |
| 11–12 — 3D/animation | DEFERRED | Interactive offices, cinematic city, and Company World animation were removed from the current delivery scope by user decision. |
| 13 — Open-source packaging | BLOCKED_ON_LICENSE_CHOICE | Sanitized artifacts, clean production installation, CycloneDX SBOM, dependency-license inventory, zero-high/critical vulnerability gate, versioned immutable releases, shared authoritative data, product LaunchAgent, hash-bound backup/restore, and receipt-bound upgrade/rollback are implemented and release-gated. Package contents explicitly exclude live data/state/logs. The project itself remains `UNLICENSED`; it cannot honestly be called open source until the owner selects and commits a license. |
| 14 — Enterprise hardening | VERIFIED_FOR_LOCAL_FIRST_V1 | The 34-gate release verifier covers durable multi-crash recovery, secret canaries, corrupt-store and symlink/path denial, collaboration persistence/API, contract-frozen Fable packets, provenance-bound dispatch receipts, non-authoritative accepted-delivery metrics, the durable self-challenge/open-question ledger, disk-full failures before and after rename, commit-uncertain tagging, bounded-concurrency/performance soak, backup/restore, clean install, upgrade/rollback, supply-chain audit, Agent Architecture migration/rollback, TypeScript/build, and twelve Playwright/Axe/visual journeys. Receipt: `1969a821ce9d91d75c62077d21c57415101da40eddaa2b9aae73938bbc104b4a`. |

The evidence-weighted original 26-section Workflow Studio blueprint estimate is **62%**. This is documented in `docs/MASTER_PLAN.md` and is not an enterprise-readiness claim.

## Workflow Studio v2 blueprint — ✅ ADVANCED VERTICAL SLICE (2026-07-13)
- Five-region studio in `web/src/WorkflowStudio.tsx`: global command bar, intent-searchable Node Library, infinite React Flow canvas, progressive Inspector, resizable execution panel, and context-aware dockable AI Architect.
- Easy outcome view, a complete five-step Guided builder, and Pro/Developer controls all edit the same canonical workflow graph.
- Workspace UX: drag/drop with quick setup, contextual node/edge/canvas menus, conditional edge labels, cycle prevention, auto-layout, minimap, fullscreen, keyboard controls, independently collapsible/resizable panels, named personal layouts, environment/project metadata, workflow-level settings, portable variables, split Run menu, and executable JSON field mapping with nested dot paths.
- AI Architect always proposes before applying and now shows node/connection diffs. Generator prompts understand the expanded executable primitive set and local-only/human-approval rules.
- Executable types expanded to 23 primitives: `input`, `file-input`, `folder-input`, `agent`, `orchestrator`, `critic`, `parallel`, `map`, `search`, `pdf-reader`, `obsidian-read`, `obsidian-write`, `check`, `if`, `delay`, `human-approval`, `python`, `shell`, `json-transform`, `read-file`, `write-file`, `http`, and `output`. Non-runtime catalog entries remain visibly marked `PLANNED`.
- Python has AI Generated/Assisted/Manual/Locked modes, an inspector editor, and a dedicated full-screen code workspace with inputs, outputs, assistant area, tests/logs/dependencies/versions/permissions surfaces. Workspace file primitives reject traversal and symlink crossings; subprocesses have timeouts and process-group cancellation but are not claimed to be an operating-system sandbox. Python dependencies require a separate reviewed `install-packages` capability and install into content-addressed cached virtual environments; the system interpreter remains untouched.
- Runtime control: deterministic Yes/No routing with skipped paths, bounded map/critic work, human approval and breakpoints, model retries, context limits, local-only enforcement, maximum duration, HTTP/process timeouts, pause/stop, actionable run events, and execution checkpoints persisted during runs.
- Workflow governance: content-addressed automatic version snapshots (deduplicated, 50-version retention), list/restore APIs and UI, audit entries for restores/approvals, secret-reference guidance, and installed AgentBrain skills attachable to model nodes.
- Added the executable 16-node **Premium 3D Website Studio** template: brand knowledge, creative direction, three concepts, human selection with reviewer direction, storyboard, UI/3D/motion/copy specialists, build, critic loop, deterministic gate, final approval, and local report artifact.
- Added `docs/WORKFLOW_STUDIO_ARCHITECTURE.md` as the canonical schema, runtime-invariant, extension, and controlled-self-improvement contract.
- Production bundle is code-split into React, workflow-canvas, Markdown, and application chunks. Verified with Node syntax check, Vite/TypeScript production build, headless Chrome visual capture at 1728×1117, direct runtime tests, and **30/30** live API smoke checks.
- The former “remaining major slices” listed here have advanced: typed ports, MCP execution, graph parallelism, secure triggers, groups/subworkflows, and retry-from-node now have real implementations and focused tests. Later-phase UX, evaluation, governance, Company World, packaging, and hardening remain open as recorded above.
- Phase 3 resource scheduling accepts validated `settings.resources` caps (`maxModelCalls`, `maxSubprocesses`, `maxHttpRequests`, `maxMcpCalls`) plus optional node-local caps at `node.runtime.resources`. The per-run coordinator remains the stricter nested ceiling. A process-global FIFO coordinator gates workflow, direct agent, chat, Architect/generator/test-node, and embedding model calls. `CC_GLOBAL_MODEL_CALLS` defaults to 2; `CC_GLOBAL_MODEL_MEMORY_GB` is optional and counts only explicit memory-byte metadata. Unknown estimates remain concurrency-bounded and are labeled unavailable. Stop, emergency stop, client disconnect, failure, timeout cleanup, and shutdown release/cancel leases. System and Operations Map show aggregate, redacted state. Host evidence: current-repository PID 36368, global 8/8, per-run 5/5, integration 3/3, records 2/2, recovery 26/26, security 4/4, and smoke 30/30. Phase 3 is verified.
- Phase 4 nested-group authoring is additive and focused-test-backed: legacy flat groups receive explicit parent/order/collapsed/disabled/node membership without losing extensions; parents load before children; nested creation and moves preserve absolute coordinates; invalid parentage is rejected before mutation; collapse and disabled state inherit recursively; group runs send the exact recursive member-node set to selected execution. Schema/group tests pass 8/8 and UI hierarchy helpers pass 8/8, with TypeScript and production build clean. The prior canonical foundation remains host-verified; this UI/run-selection increment still needs host-controller recovery/security/smoke verification because the managed worker cannot restart or connect to loopback. Phase 4 remains partial because reusable-template UX and subworkflow safety-ceiling inheritance are still open.
- Phase 4 subworkflow safety ceilings are additive and host-verified: child workflows take the minimum parent/child/node resource policy, cannot restore a denied executable capability or local-only escape, reject recursive/over-depth calls before child side effects, persist redacted parent/child policy evidence, reconstruct that ceiling for retry/restart recovery, and receive parent pause/stop propagation. Pure context 7/7, schema 8/8, ports 5/5, scheduler 5/5, resources 5/5, global resources 8/8, group helpers 8/8, recovery 33/33, security 4/4, smoke 30/30, TypeScript, build, and syntax pass. Reusable component-library and comment UX remain open.
- Phase 4 version-pinned subworkflows are additive and host-verified: extraction records the actual current child snapshot; exact definitions resolve through hash/id/workflow integrity checks before child-run side effects; pins persist in run/checkpoint/retry records; referenced snapshots bypass normal retention pruning; and missing or mismatched pins fail with explicit review/update guidance. The inspector shows the pinned child name/version and advances only through a confirmed action using normal undo/save behavior. Version-pin helpers/store pass 8/8, schema 8/8, ports 5/5, context 7/7, scheduler 5/5, resources 5/5, global resources 8/8, recovery 35/35, security 4/4, smoke 30/30, localhost HTTP 200, TypeScript, build, syntax, and diff checks.
- Phase 4 reusable components are additive and host-verified: extracted children carry explicit reusable metadata; the read-only catalog returns only identity, description, node count, exact current snapshot identity, and availability; restored definitions select their active snapshot rather than the newest timestamp; and the Node Library inserts only available components with an exact pin through undo/save draft behavior. Rename preserves IDs, extensions, and parent pins. Archive is non-destructive and rejects referenced components with impacted-parent names. Component tests pass 8/8; schema 8/8, ports 5/5, version pins 8/8, context 7/7, scheduler 5/5, resources 5/5, global resources 8/8, group helpers 8/8, recovery 35/35, security 4/4, smoke 30/30, localhost HTTP 200, TypeScript, production build, syntax, and diff checks pass.
- Phase 4 canvas comments are additive and host-verified: canonical comments preserve stable safe IDs, text, absolute position, optional semantic node/group anchors, resolution state, and unknown extensions. They render as visibly review-only notes in the full canvas, have truthful Easy-mode disclosure, and support create/edit/drag/anchor/resolve/reopen/delete through the existing undo/save path. Missing anchors are warned and preserved rather than retargeted. Comments round-trip through saves, portable JSON duplication/import, and exact version restore, while staying outside nodes, edges, ports, group-selected runs, topology, artifacts, inputs, outputs, and execution context. Comment helpers pass 10/10; schema 8/8, ports 5/5, groups 8/8, version pins 8/8, reusable components 8/8, context 7/7, scheduler 5/5, resources 5/5, global resources 8/8, recovery 35/35, security 4/4, smoke 30/30, localhost HTTP 200, TypeScript, build, syntax, and diff checks pass.
- Phase 4 portable reusable-component manifests are additive and host-verified: a versioned deterministic manifest carries only the selected reusable root and its exact nested pinned closure, canonical definitions, hashes, compatibility, and allowlisted provenance. Export refuses secret-bearing workflows; validation rejects tampering, unsafe IDs, missing or conflicting pins, unrelated payloads, cycles/depth overflow, incompatibility, and collisions. Staging persists a review proposal without workflow/version writes. Approval revalidates and installs only new development/import-review definitions; existing and locked-production IDs cannot be overwritten. Reusable Library controls expose export, file import, executable/dependency/conflict summaries, and explicit approve/reject. Pins, comments, and safe unknown extensions round-trip in the deterministic suite, and a live browser journey proves export, staged review, approval, canonical restoration, and library visibility.
- Phase 5 MCP registry/client security is additive and host-live verified: existing safe definitions migrate without loss; IDs, transport, commands, URLs, scopes, and schemas are validated; duplicates never overwrite; newly registered local executables remain disabled/untrusted until explicit review. Stdio children receive only documented allowlisted environment keys. Discovery persists bounded redacted schemas with server/version/transport provenance, and server/tool scopes compose deny-wins with workflow/node permissions. Calls normalize timeout/cancellation/malformed/remote/transport errors and persist only bounded redacted metadata evidence, never arguments, headers, auth values, or tool output. Evidence: registry/client 10/10, live transports 4/4, security 4/4, recovery 36/36 (including pre-review denial/post-review execution), smoke 30/30, localhost HTTP 200, schema 8/8, ports 5/5, TypeScript, production build, Node syntax, and diff checks.
- Phase 5 trigger durability slice 5T-A is host-verified. Canonical trigger definitions live only on their owning workflow behind a version-preserving lock-aware adapter; stale ordinary saves preserve server-owned triggers. The versioned operational store checksum-backs up legacy files, fails closed on corruption, serializes fsync+rename mutations, reserves before launch, separates history from identity retention, and persists exact redacted workflow/version/run provenance. Server-authenticated run idempotency reconciles reserved/starting work across restart. Evidence: store 14/14; live trigger/concurrent delivery/pagination/redaction 6/6; actual LaunchAgent reserved/starting/two-restart recovery 4/4; restart 2/2; schema 8/8; security 4/4; recovery 36/36; smoke 30/30; localhost HTTP 200; TypeScript/build/syntax/diff clean. Pre-migration backup and adjacent store backups have matching hashes.
- Phase 5 schedule slice 5T-B is host-verified. Five-field cron parsing rejects malformed ranges/lists/steps, IANA timezones are explicit, and UTC is the documented default. Interval/cron cursors, definition hashes, queued-one state, and suppression reasons persist in the operational store; cursor advancement and intended-instant reservation share one atomic mutation. Misfire `skip|fire-once`, overlap `skip|queue-one|allow`, and bounded catch-up are explicit canonical policies. DST rules skip nonexistent local minutes and treat repeated local minutes as distinct UTC instants; day-of-month/day-of-week use documented OR semantics when both are restricted. Invalid legacy schedules are retained disabled for review, while new mutations fail actionably. Host evidence on 2026-07-13: persistent pre-reload backup `/Users/kedabektechlabs/command-center-backups/20260713T125955Z-trigger-5tb-host`; current-repository LaunchAgent; localhost HTTP 200; schedule 13/13; store 14/14; live triggers 6/6; restart 3/3; schema 8/8; security 4/4; interrupted recovery 36/36; smoke 30/30; TypeScript, production build, and diff checks passed. The restart test uses a future scheduled instant so host speed cannot create a minute-boundary race. Disposable schedule fixtures were removed and the operational store reports zero remaining schedules.
- Phase 5 folder slice 5T-C is host-verified. The deterministic controller created `/Users/kedabektechlabs/command-center-backups/20260713t180000z-trigger-5tc-controller`, verified manifest SHA-256 `8a448777af7c8de33dedbaa37ef86da2153f26848345acacd5063f2d6bbcee26`, proved zero active workflows, reloaded `com.local.commandcenter`, and verified HTTP 200. The first live API gate found a real response/baseline race; trigger creation now awaits its initial folder snapshot before acknowledging the caller, preventing an immediate file from being absorbed silently. Revalidation passed: folder 9/9, service 2/2, store 14/14, schedule 13/13, live trigger API 6/6, real LaunchAgent restart 5/5, schema 8/8, security 4/4, interrupted recovery 36/36, smoke 30/30, TypeScript, production build, diff, and localhost. The restart fixture proves stable candidate, delivery, and run identity across two restarts with exactly one launch and owned-state cleanup.
- Phase 5 secure-webhook slice 5T-D is host-verified. New credentials are disclosed once as a bearer token for a token-free endpoint; the URL-token route is retained only as deprecated compatibility. Strict hash-only records migrate legacy hashes, and token verification plus delivery reservation commit atomically. Expected-revision rotation serializes concurrent callers, returns plaintext only to the first application, invalidates the previous token, and uses a persisted hashed operation key for safe replay. Revocation removes authority permanently across disable/re-enable and restart. Workflow locks and deny-wins `receive-webhooks` permission protect authority-adding operations; safe-reducing revoke remains permitted by the permission check but still respects the canonical lock mutation. Public lists expose only active/revoked and revision. Workflow Settings provides Rotate/Revoke plus an ephemeral Copy/Dismiss disclosure card that never places credentials in workflow state, notices, storage, history, or audit. The live gate also exposed a controller postcondition race; `service.reload` now returns only after `/api/system` is healthy. Evidence: controller 7/7, store 17/17, live triggers 10/10, trigger restart 4/4, live recovery 4/4, folder restart 5/5, schedule 13/13, schema 8/8, security 4/4, restart recovery 1/1, smoke 30/30, TypeScript, build, syntax, diff, and localhost HTTP 200.
- Phase 5 auth-reference slice is host-live verified. Opaque IDs and versioned metadata persist separately from macOS Keychain values; corrupt metadata and missing/denied Keychain entries fail closed. No-store CRUD requires explicit mutation intent, optimistic revision matching, and unused-reference proof for deletion. Workflow schema rejects arbitrary or secret-bearing reference objects. Remote MCP registration/update/migration rejects inline headers/environment credentials, while HTTP nodes reject inline sensitive headers, URL userinfo, and secret-named query values. Bearer values are injected only in remote HTTP/MCP request options and never in canonical objects or evidence. Provider children no longer inherit the entire host environment. The UI manages Keychain references, reports status/usage, and selects references for HTTP/MCP without redisplaying values. Evidence: registry 4/4, API 5/5, runtime 5/5, provider environment 1/1, schema 9/9, MCP 12/12, real Keychain HTTP/MCP/production-lock/permission/no-persistence 5/5, existing security 4/4, live MCP 4/4, restart 1/1, smoke 30/30, TypeScript/build/diff, controller reload receipt `32b8de03c461e94af4a41ba1a1e8a30d63f294196b137921d8f31aedf74eb8bd`, and localhost HTTP 200.
- Continuous Intelligence and Controlled Evolution is now a required dependency-gated subsystem documented in `docs/CONTINUOUS_EVOLUTION.md`. It adds external intelligence and internal optimization departments, but implementation must follow verified durable runtime, evaluation, permission, and environment-promotion foundations. It cannot silently self-modify production.

## Phase 0 — Audit, backup, smoke tests, baseline — ✅ DONE (2026-07-12)
- Backup: `~/command-center-backups/20260712-phase0/` (repo + AgentBrain + opencode config), 62 MB.
- Smoke tests: `scripts/smoke.sh` — 22 checks, all PASS (endpoints, path-traversal block, secret-leak checks).
- Audit docs: `docs/FEATURE_AUDIT.md`, `docs/CURRENT_ARCHITECTURE.md` (see `FEATURES.md` for full detail).
- Baseline: git tag `baseline-v0` at commit of this state.
- Full 9-doc audit suite: pending scope confirmation (see ROADMAP).

## Scope decision (2026-07-12)
Confirmed: build the **spine on current JSON backend**; **scoped-down** website template; **full access for owner, safe-by-default permission system for the product**.

## Phase 1a — Complexity modes + Home + adaptive nav — ✅ DONE (tested)
- Global Easy/Guided/Pro/Developer selector (persisted, presentation-only). `useMode()` + `<AtLeast>` gate.
- Adaptive sidebar (primary + collapsible Advanced Tools in Easy/Guided; flat in Pro/Dev).
- Home / AI Overview: NL command box → AI Build handoff, status cards, plain-language "needs attention", starters.
- Verified: tsc clean, 22/22 smoke, no backend change.

## Phase 1b — Canvas UX + progressive node inspector — ✅ DONE (tested)
- Canvas: MiniMap (Pro+), Auto-layout, Fit-to-view, snap-to-grid, plain-language node subtitles (Easy/Guided) + tooltips.
- Progressive tabbed inspector (Basic always; Advanced/Permissions/Test at Pro+). Easy/Guided see only essentials with friendly labels.
- Real single-node Test tab (`POST /api/workflows/testnode`) — verified returns output.
- Permissions tab = honest plain-language summary (no fake controls).
- Verified: tsc clean, 22/22 smoke, testnode endpoint returns real output.
- Deferred (marked, not faked): undo/redo, command palette, node grouping, sticky notes.

## Phase 2 — AI Architect — ✅ DONE (tested)
Context-aware drawer in Workflow Studio. `POST /api/architect` → {reply, workflow?}. Quick commands + free text; explanations inline; changes shown as +/−/~ diff cards; Apply/Reject/Undo; never silently restructures. Verified: explain + add-reviewer produce correct reply/proposal.

## Phase 3 — Preflight + Safe Run — ✅ DONE (tested)
Preflight modal with real checks (missing input/output, unset/uninstalled models, missing cloud key, unreachable nodes) + Fix-automatically + Ask-Architect. Safe preview clamps critic loops→1 and parallel→1 branch (verified in feed).

## Phase 4 — Simple run view + error cards — ✅ DONE (tested)
Mode-aware run view: simple stage-story + progress (Easy/Guided) vs detailed log (Pro/Dev), switchable mid-run. Plain-language error card (what/why/nothing-damaged/next actions).

## Phase 5 — Template Center + onboarding + scoped website — ✅ DONE (tested)
`GET /api/templates` catalog (Research/Documents/Business/Coding/Websites), outcome-organized cards with metadata + Use/Open-in-canvas. First-run onboarding (use-case + default mode, skippable, restartable via localStorage `cc-onboarded`). Scoped "Simple Website Builder" verified end-to-end → produced a real 13 KB index.html.

## SPINE COMPLETE ✅ (Phases 0–5)

## Phase 6 — Dependability & Results — ✅ DONE (tested)
Run Center + Artifact Center; pause/resume (workflow, node boundaries) + cancel (both). Unified /api/runs, run detail, /api/artifacts. Verified full lifecycle + artifacts populate.

## Phase 7 — Model Policies — ✅ DONE (tested)
policy:fast/balanced/premium/local resolve per-run to concrete models. New nodes default to policy:balanced. Verified fast→gpt-oss-20b.

## Phase 8 — Knowledge & Memory (RAG) — ✅ DONE (tested)
Ingest text+PDF → chunk → embed (nomic 768d) → cosine search. Search node in workflows. Knowledge page. Verified grounded RAG answer.

## Phase 9 — Tools & MCP — ✅ DONE (tested)
Tools page: built-in capability cards + MCP server registry (writes opencode.json mcp). Verified config write. (Live server needs the binary installed — noted honestly.)

## Phase 10 — Security & Governance — ✅ DONE (tested)
Per-agent permission presets (full/standard/readonly → real opencode perms), 🛑 Stop-all emergency, audit log + Activity view. Verified readonly perms + killall + audit.

## Phase 11 — Evaluations — ✅ DONE (tested)
Deterministic Check node (contains/minLength/isJson) — pass/needs-review, no AI-judging-AI. Verified flagging.

## Phase 12 — UX polish — ✅ DONE (⌘K command palette + canvas undo/redo)
## Phase 15 — Skills Library — ✅ DONE (attachable playbooks; verified injection)
## Phase 14 — Durability (slice) — ✅ DONE (persist-at-start + restart reconcile → interrupted; verified)
## Phase 16 — Video generation — ✅ DONE & VERIFIED
LTXV (ltxv-2b-0.9.6-distilled + t5xxl_fp8) in ComfyUI. Studio Video tab gated on real model presence. Verified: 49-frame 768×512 coherent on-prompt animated WebP in ~14s local. Endpoint /api/studio/video; buildVideoWorkflow uses CheckpointLoaderSimple + CLIPLoader(ltxv) + EmptyLTXVLatentVideo + LTXVConditioning + KSampler + SaveAnimatedWEBP.

## Phase 13 — SQLite + full modular backend — DEFERRED (architect recommendation)
Not done deliberately. JSON passes 22/22 at current scale; a full rewrite is pure regression risk with no user-visible gain. Recommend a dedicated focused session with migration + tests when data actually demands it. Full mid-run checkpoint-resume (14) is technically infeasible (opencode subprocesses die on restart) — the honest durability slice is shipped instead.

## STATUS: Phases 0–12, 14(slice), 15, 16 complete & verified. Only Phase 13 (SQLite rewrite) deferred by design.

## Historical roadmap note
The original spine roadmap in `docs/IMPLEMENTATION_ROADMAP.md` is now substantially implemented and was superseded by the Workflow Studio v2 vertical slice documented at the top of this file. SQLite remains deliberately deferred; the remaining product slices are listed there and in `docs/WORKFLOW_STUDIO_ARCHITECTURE.md`.
