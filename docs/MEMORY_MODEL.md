# Memory Model

Knowledge, transient run context, reusable memory, and immutable provenance are distinct concepts. AgentBrain/Obsidian-compatible notes currently provide local knowledge and memory sources. Workflows may read/write explicit vault paths, while runtime outputs and artifacts retain run provenance.

Memory deletion, contradiction workflows, retention policies, embedding versioning, and permission-scoped memory views remain incomplete and must not be represented as verified.
