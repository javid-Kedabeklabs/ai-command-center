# AI Command Center — Current Public Review Snapshot

This branch is a sanitized source snapshot for independent review.

- Local source checkpoint: `d0f7c04` (`Reconcile hardened v1 behavior and evidence`).
- Runtime data, audit logs, profiles, generated stores, credentials, local models, and user content are excluded.
- This checkpoint adds exact evaluation datasets and governed learning, portable reviewed plugins, durable Company World departments, versioned install/rollback, deterministic keyboard/accessibility/visual coverage, disk-full semantics, bounded soak/performance gates, and documentation reconciled to observed behavior.
- The authoritative release contract is `npm run verify:release` (26 gates). The package remains `UNLICENSED`; this is a source-available review snapshot, not an open-source release.
- Treat `docs/COMPLETION_AUDIT.md`, `docs/IMPLEMENTATION_STATUS.md`, and current source/tests as authoritative rather than older review commits.

Review branch: `review/current-20260713`.
