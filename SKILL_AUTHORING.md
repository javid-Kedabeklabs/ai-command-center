# Skill Authoring

A skill package describes a repeatable capability: purpose, version, instructions, inputs, outputs, required tools and permissions, dependencies, examples, tests, evaluation rubric, compatibility, license, provenance, and failure behavior.

Skills guide agents; they do not silently grant permissions. Updates create new versions, run regression/evaluation suites, present a human-readable diff, and require approval before production promotion.
